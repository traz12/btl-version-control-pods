apiVersion: v1
kind: Service
metadata:
  name: breaker
  namespace: breaker-test
  labels:
    app: breaker
    environment: test
spec:
  type: ClusterIP
  selector:
    app: breaker
  ports:
    - name: http
      port: 80
      targetPort: 8080
