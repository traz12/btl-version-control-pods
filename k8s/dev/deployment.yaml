apiVersion: apps/v1
kind: Deployment
metadata:
  name: breaker
  namespace: breaker-dev
  labels:
    app: breaker
    environment: dev
    # Filled in by the deploy workflow, so `kubectl get` shows the version.
    app.kubernetes.io/version: "${VERSION}"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: breaker
  template:
    metadata:
      labels:
        app: breaker
        environment: dev
        app.kubernetes.io/version: "${VERSION}"
    spec:
      containers:
        - name: breaker
          # The deploy workflow fills this in with the image it just pushed.
          image: ${IMAGE}
          ports:
            - containerPort: 8080
          env:
            - name: ENVIRONMENT
              value: "dev"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
